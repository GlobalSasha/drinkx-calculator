# Current Status — 2026-05-21

## Session context

The user is closing the current Codex session and asked to save documentation and current status so work can continue later.

## Project path

```text
/Users/aleksandrhvastunov/drinkx-calculator
```

Start future work from:

```text
/Users/aleksandrhvastunov/drinkx-calculator/index-ares.html
```

## Live URL

```text
https://globalsasha.github.io/drinkx-calculator/index-ares.html
```

## GitHub repository

```text
https://github.com/GlobalSasha/drinkx-calculator
```

Remote:

```text
origin https://github.com/GlobalSasha/drinkx-calculator.git
```

## Last completed work

Completed before this status file:

- Audited financial formulas against CSV export.
- Confirmed arithmetic consistency between code and CSV.
- Identified that high DrinkX profit mostly comes from assumptions:
  - higher DrinkX cups/day;
  - lower DrinkX OPEX;
  - zero DrinkX fraud vs classic machine fraud.
- Added shareable calculation links to `index-ares.html`.
- Share button label: `↗ ССЫЛКА`.
- Share payload is stored in URL hash as `#calc=<payload>`.
- Shared links restore:
  - all classic-machine inputs;
  - all DrinkX inputs;
  - selected period;
  - tax toggle.
- Pushed share-link change to GitHub.

Last pushed commit before documentation:

```text
02d8757 Add shareable calculator links
```

## Verification already done

Local browser check with Playwright:

- Page opened at `http://localhost:5175/index-ares.html`.
- New `↗ ССЫЛКА` button was visible.
- Generated shared URL with modified values:
  - classic cups/day = `23`;
  - DrinkX cups/day = `47`;
  - period = `60`;
  - taxes = `true`.
- Reopened the generated URL.
- Confirmed restored state:
  - `stdCups: "23"`;
  - `drxCups: "47"`;
  - `period: 60`;
  - `taxes: true`;
  - tax button visually active.

GitHub Pages was checked after push and contained the new share-link code.

## Current documentation added in this session

- `README.md`
- `docs/PROJECT_DOCUMENTATION.md`
- `docs/STATUS_2026-05-21.md`

These files were added as the final documentation package for this session.

## Open questions / next likely work

The user said "пока не нужно" about changing formulas. Do not change formula logic unless asked.

Likely next feature:

- Add a "fair comparison" toggle.
- When enabled, sync cups/day between classic machine and DrinkX.
- Optionally show a factor breakdown explaining why DrinkX wins or loses.

Formula concern to keep in mind:

The current `profitPark` formula is correct for the current model, but it compares whatever inputs are entered. If DrinkX has twice as many cups/day as the classic machine, DrinkX will naturally show much larger profit.

## Commands to resume later

```bash
cd /Users/aleksandrhvastunov/drinkx-calculator
git status --short
git log -3 --oneline
python3 -m http.server 5175
```

Open:

```text
http://localhost:5175/index-ares.html
```
